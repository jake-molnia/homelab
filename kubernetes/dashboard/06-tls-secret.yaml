apiVersion: v1
kind: Secret
metadata:
  name: dashboard-tls
  namespace: kubernetes-dashboard
type: kubernetes.io/tls
data:
  # You will need to replace these with your actual base64-encoded certificate and key
  tls.crt: ""
  tls.key: ""
