# applications/kasm.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kasm-workspaces
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  project: default
  source:
    repoURL: https://github.com/kasmtech/kasm-helm
    path: kasm-single-zone
    targetRevision: release/1.17.0
    helm:
      values: |
        global:
          hostname: kasm
        ingress:
          enabled: true
          className: tailscale
          hosts:
            - host: kasm
              paths:
                - path: /
                  pathType: Prefix
          tls:
            - hosts:
                - kasm
        persistence:
          enabled: true
          # Note that the 'uploads' PVC is configured under a generic 'extra' key
          extra:
            - name: uploads
              storageClassName: longhorn
              accessMode: ReadWriteOnce
              size: 50Gi
              mountPath: /uploads
        database:
          host: "shared-postgresql.database.svc.cluster.local"
          port: 5432
          sslMode: "prefer"
          database: "kasm"
          username: "kasm"
  destination:
    server: https://kubernetes.default.svc
    namespace: kasm
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
